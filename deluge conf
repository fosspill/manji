IP=`ifconfig | grep -o "tun[0-9]" | xargs ifconfig | grep -oE "([0-9]{1,3}\.){3}[0-9]{1,3}" | head -n 1`
logger -t "Deluge Use IP" $IP
/usr/local/bin/deluge-console -c /mnt/config/deluge/confdir "config --set listen_interface $IP" | logger -t "Deluge Use IP"
